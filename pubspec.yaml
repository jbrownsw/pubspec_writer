name: pubspec_writer
description: Simple command line utility for upgrading packages in your pubspec.yaml file.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  args: ^2.3.1
  path: ^1.8.2
  yaml: ^3.1.1
  yaml_edit: ^2.0.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.4
